<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Table name for Eloquent blog entries
    |--------------------------------------------------------------------------
    |
    | Blog entries are stored in a table called `blog_entries` by default. If
    | you want another name for the table you can set it here. This table
    | name will be used by the registered migrations and the BlogEntry
    | model (as well as its descendants, should you extend it).
    |
    */

    'entries_table' => 'blog_entries',

    /*
    |--------------------------------------------------------------------------
    | Database connection name for Eloquent blog entries
    |--------------------------------------------------------------------------
    |
    | Blog entries are stored in a table on the default database connection.
    | If you want to use another connection you can set its name here. This
    | connection will be used by the registered migrations and the
    | BlogEntry model (as well as its descendants, should you
    | extend it).
    |
    */

    //'entries_connection' => 'pgsql',

];
